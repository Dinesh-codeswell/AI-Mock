# Root-level ignores for a clean GitHub push

# Dependencies
node_modules/

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# OS and editor cruft
.DS_Store
Thumbs.db

# Env and secrets (commit a .env.example instead)
.env*

# Vercel/CLI state (if used at root)
.vercel/

# TypeScript build info caches
*.tsbuildinfo

# Optional: ignore local workspace JSON docs not used by the app
Why.json
design.json
new.json
news.json